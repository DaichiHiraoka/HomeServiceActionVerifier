# 現システムの技術説明

この文書は、現在の研究システムについて技術的に聞かれたときに説明するための資料である。

この文書は現在の仕様と連動して更新する。入力、モデル、評価方法、保存するデータ、使わない情報が変わった場合は、この文書も必ず修正する。

## 1. システムの目的

本システムの目的は、個人宅で無人作業を行う作業者について、作業対象外の物品操作の兆候を早めに検知することである。

対象とするのは、すべての不審行動ではない。

まずは、次のような限定された行動に絞る。

- 落ちている物を拾う
- 棚や机の上の物に手を伸ばす
- 物を持ち上げる
- 持った物を身体側へ近づける
- 持った物を元に戻さず移動する

技術的には、「物を拾う」という単発動作を当てるだけではなく、その前後の流れを見て、通常作業として自然か、不自然な系列かを判定する。

## 2. 使う情報

本システムでは、人に関する情報と物体に関する情報を分けて扱う。

| 種類 | 使う情報 | 目的 |
| --- | --- | --- |
| 人物情報 | 骨格座標、関節角度、手先位置、体幹方向、移動量 | 作業者の姿勢と動きを見る |
| 物体情報 | 対象物体周辺の切り出し画像、bbox、中心座標、移動量 | 物体が何で、どこへ動いたかを見る |
| 文脈情報 | 前後数秒の行動列、作業対象物、作業エリア | 今の行動が作業として自然かを見る |

人物については、顔画像や服装などの外観情報を主な入力にしない。

物体については、部屋全体ではなく、対象物体とその周囲だけを扱う。

## 3. 使わない情報

現仕様では、次の情報を主入力として使わない。

- 顔画像
- 年齢、性別、体型などの人物属性
- 作業者の服装特徴
- 部屋全体の画像理解
- 室内全体の常時保存映像
- VLM による全体画像の自由な解釈

理由は、個人宅で扱う情報をできるだけ少なくし、どの情報が判定に効いたのかを説明しやすくするためである。

## 4. 全体の処理の流れ

本システムでは、「作業票を人間が決めること」と「逸脱をシステムが検知すること」を分離する。

人間が定義するのは、作業前の許可条件である。映像中に何が起きたかは、骨格・姿勢推定とYOLOトラッキングからシステムが自動生成する。

```text
動画またはカメラ入力
  ├─ 人物姿勢推定
  │    └─ 骨格座標、手首、体幹、移動量、姿勢ラベル
  │
  └─ YOLO物体検出 + トラッカー
       └─ 物体ID、bbox、中心座標、移動量、可視状態、zone
                ↓
人物・物体の時刻同期
                ↓
観測イベント生成
  approach / contact / lift / carry / place / return / disappear / exit
                ↓
物体ごとの状態遷移列を生成
                ↓
作業票から生成した「許可された状態遷移」と照合
                ↓
逸脱種別、根拠、信頼度、不審度スコアを出力
                ↓
review / warning / high_risk
```

YOLOは違反行為を判定しない。YOLOの役割は、物体の種類、位置、ID、移動、消失候補を供給することである。

骨格・姿勢推定も、それ単独では違反を判定しない。人物の手がどの物体へ近づいたか、持ち上げた物体とともに移動したかを推定するために使う。

違反または逸脱の判定は、両者から生成された時系列イベントと、作業票に記載された許可条件との不一致によって行う。

現在の `grip_detector` プロトタイプも、この方針に合わせる。単に手が閉じて物体bboxと重なった状態を最終判定にせず、手骨格による把持候補、手と物体の接触候補、追跡物体の速度または初期位置からの変位が同時に成立した場合を「把持中に物体を移動させた」イベントとして扱う。

## 5. 骨格・姿勢の取得

骨格・姿勢の取得では、動画内の人物から関節点を取り出す。

例として、次のような点を使う。

- 肩
- 肘
- 手首
- 腰
- 膝
- 足首
- 頭部方向

そこから、次の特徴を計算する。

- 手首の位置
- 手首の速度
- 体幹の向き
- しゃがみ姿勢かどうか
- 手を伸ばしているか
- 物体に近づいているか
- 物体を持ったまま移動しているか

実装時の姿勢推定器は、現時点では固定しない。候補は MediaPipe Pose、YOLO 系 pose 推定、OpenPose 系などである。

研究として重要なのは、どの姿勢推定器を使うかよりも、出力された骨格系列から文脈判定を行う点である。

## 6. 物体の扱い

骨格だけでは、「何を拾ったか」「拾った物がどこへ行ったか」が分からない。

そのため、対象物体については別に扱う。

物体について取得する情報は次の通りである。

- bbox
- 中心座標
- フレーム間の移動量
- 手首との距離
- 物体が見えなくなったか
- 元の位置に戻ったか
- 作業エリア外へ移動したか

対象物体の画像は、部屋全体ではなく、物体とその周囲だけを切り出す。

この切り出し画像は、物体が作業対象か、私物らしいか、移動したかを判断するために使う。

## 7. 物体トラッキング

物体トラッキングでは、YOLOの検出結果にBoT-SORTまたはByteTrackを接続し、同じ物体をフレーム間で同一IDとして追跡する。

物体ごとに次の時系列を保持する。

- `object_id`
- 物体ラベル
- bboxと中心座標
- 検出信頼度
- 所属zone
- フレーム間移動量
- 手首との距離
- 胴体との距離
- 可視・不可視状態
- 初期位置からの距離
- 作業終了時に初期位置へ戻ったか

トラッキングの目的は、単に物体を囲むことではない。次の状態変化を判定可能にすることである。

- 手が物体へ接近した
- 接触後に物体が移動した
- 物体と手首が同じ方向へ移動した
- 物体が身体側へ運ばれた
- 物体が別zoneへ移動した
- 物体が元の位置へ戻された
- 物体が遮蔽または画面外移動によって見えなくなった

## 8. 逸脱を検知する仕組み

### 8.1 人間が決めるもの

人間が作業前に定義するのは、個々の映像イベントではなく、作業上の許可条件である。

作業票には、少なくとも次を記述する。

| 項目 | 内容 |
| --- | --- |
| 作業対象物 | ルーター、LANケーブル、電源アダプタなど |
| 作業者所有物 | ドライバー、テスター、工具バッグなど |
| 保護対象物 | 財布、鍵、住人のスマートフォンなど |
| 許可zone | 作業場所、工具置場、入退室経路 |
| 禁止・要確認zone | 私物机、収納、寝室など |
| 許可行動 | 接近、接触、持ち上げ、移動、設置、復元 |
| 行動条件 | どの物体を、どのzone内で、どこへ移動してよいか |
| 復元条件 | 作業終了までに元の位置へ戻す必要があるか |
| 時間条件 | 接触・保持・作業エリア外滞在の許容時間 |
| 警告条件 | 即時警告、人手確認、記録のみの区分 |

人間は「財布を拾った」という観測結果を入力しない。人間が決めるのは、「財布は作業対象外であり、接触または移動が発生した場合は要確認とする」という方針である。

YOLOのクラスラベルだけでは、所有者や作業上の役割は決まらない。同じ`phone`でも、住人の端末と作業者の端末は別物である。

そのため、作業開始前に保護対象物と作業対象物を登録し、初期位置、物体ラベル、局所画像特徴を使って最初の`track_id`へ対応付ける。作業中にIDが途切れた場合は、ラベル、位置、局所画像特徴、消失時間を用いて再対応付けする。役割が確定できない物体は`unknown`として扱い、自動で私物または作業対象物へ決めつけない。


### 8.2 システムが映像から生成するもの

各時刻 `t`、各物体 `o` について、システムは骨格系列とYOLOトラックから次の観測量を計算する。

| 観測量 | 算出方法 |
| --- | --- |
| 手先距離 | 左右手首と物体bboxの最短距離 |
| 接近傾向 | 手先距離が連続フレームで減少しているか |
| 接触候補 | 手先距離が閾値以下の状態が一定時間続いたか |
| 物体移動 | 物体中心が初期位置または直前位置から移動したか |
| 把持中移動 | 手の閉じ姿勢、物体接触候補、物体速度または初期位置からの変位が同時に成立したか |
| 手・物体同期 | 手首と物体の移動方向・速度が一致しているか |
| 身体側移動 | 物体と胴体中心の距離が縮小したか |
| zone遷移 | 物体または人物がどのzoneからどのzoneへ移ったか |
| 復元 | 物体が初期位置の許容範囲内へ戻ったか |
| 消失 | 追跡中の物体が一定時間検出されなくなったか |
| 観測信頼度 | 姿勢推定、検出、トラッキングの信頼度 |

単一フレームだけではイベントを確定しない。一定フレーム数の継続、手と物体の同期、直前状態との整合性を確認してからイベント化する。

実装上の現行プロトタイプでは、`object_contact_score`、`object_motion_score`、`object_speed_px_s`、`object_displacement_px` をCSVに保存する。これにより、誤警告が「握り姿勢」「手とbboxの重なり」「物体移動」のどこで生じたかを後から確認できる。

### 8.3 物体ごとの状態遷移

各物体は次の状態を持つ。

```text
idle
  ↓
approached
  ↓
contacted
  ↓
lifted
  ↓
carried
  ├─ placed
  │    └─ returned
  ├─ disappeared
  └─ exited
```

状態の意味は次の通りである。

| 状態 | 判定条件 |
| --- | --- |
| `idle` | 手との相互作用がない |
| `approached` | 手先距離が継続的に減少し、手を伸ばす姿勢がある |
| `contacted` | 手先が物体近傍に一定時間滞在する |
| `lifted` | 接触後に物体が移動し、手首と物体が同期する |
| `carried` | 物体を保持した状態で人物が移動する |
| `placed` | 物体移動が停止し、手が離れる |
| `returned` | 初期位置または指定位置へ戻る |
| `disappeared` | 保持中または身体近傍で追跡が途切れる |
| `exited` | 許可zone外または監視領域外へ移動する |

この状態遷移を物体IDごとに保持することで、「拾った」という単発動作ではなく、「近づく、触る、持ち上げる、身体側へ運ぶ、見えなくなる」という系列を扱う。

### 8.4 作業票を許可状態遷移へ変換する

作業票は、物体ごとの許可状態遷移として扱う。

ルーター修理の例を示す。

| 物体 | 許可される系列 | 許可されない系列 |
| --- | --- | --- |
| ルーター | `approached → contacted → lifted → placed → returned` | 作業zone外への`carried → exited` |
| ドライバー | `approached → contacted → lifted → carried → placed` | 私的zoneへの放置 |
| 財布 | 接近のみ、または接触せず通過 | `contacted`以降の全操作 |
| 鍵 | 接近のみ、または接触せず通過 | `lifted → carried`、`disappeared`、`exited` |

逸脱は、次のいずれかが成立したときに検出する。

1. **対象逸脱**  
   作業対象外物体に、許可されていない接触・移動が発生した。

2. **zone逸脱**  
   人物または物体が、作業票で許可されていないzoneへ移動した。

3. **行動逸脱**  
   その物体に許可されていない行動イベントが発生した。

4. **遷移逸脱**  
   個々の行動は許可されていても、順序が許可系列と一致しない。

5. **移動先逸脱**  
   物体が指定位置ではなく、身体側、工具バッグ側、作業エリア外へ移動した。

6. **復元逸脱**  
   一時移動が許可されていても、指定時刻までに元の位置へ戻っていない。

7. **消失逸脱**  
   物体が保持中または身体近傍で見えなくなり、再出現・復元が確認できない。

8. **時間逸脱**  
   接触、保持、禁止zone滞在が許容時間を超えた。

### 8.5 判定ロジック

逸脱判定は、固定加点だけではなく、次の順序で行う。

```text
1. 観測信頼度を確認する
2. 骨格と物体トラックからイベントを生成する
3. 物体ごとの状態遷移を更新する
4. 作業票の許可遷移と照合する
5. ハード違反とソフト逸脱を分ける
6. 複数の根拠を統合して不審度を計算する
7. 根拠と時刻を付けて出力する
```

ハード違反は、条件成立時点で強く扱う。

- 保護対象物が作業zone外へ移動した
- 保護対象物が工具バッグ付近で消失した
- 禁止zone内で保護対象物を持ち上げた
- 作業終了時点で高リスク物体が復元されていない

ソフト逸脱は、単独では警告を確定しない。

- 作業対象外物体へ手が近づいた
- 私的zone付近に一定時間滞在した
- 物体を身体側へ近づけた
- 一時的にトラックが途切れた

ソフト逸脱は、複数条件の連続成立によって警告へ昇格させる。

不審度は、概念的には次の要素から構成する。

```text
suspicion_score
  = 対象逸脱
  + zone逸脱
  + 行動・遷移逸脱
  + 移動先・復元・消失逸脱
  + 継続時間
  × 観測信頼度
```

実際の出力では、数値だけでなく根拠を必ず保持する。

```text
object_id: wallet_8
state_sequence: approached -> contacted -> lifted -> carried -> disappeared
violations:
  - protected_object_contact
  - unauthorized_lift
  - moved_toward_torso
  - disappeared_while_carried
evidence_confidence: 0.91
prediction: high_risk
```

### 8.6 正常系と逸脱系の具体例

#### 正常なルーター作業

```text
router_1:
approached
  -> contacted
  -> lifted
  -> carried within work_area
  -> placed
  -> returned
```

ルーターは作業対象物であり、移動先も許可zone内で、最終的に指定位置へ戻っている。このため正常と判定する。

#### 私物を一時的に避けて戻す場合

```text
resident_phone_2:
approached
  -> contacted
  -> lifted
  -> placed near original position
  -> returned
```

私物への接触は要確認条件になるが、短時間であり、移動先が近く、復元も確認できる。したがって即時に高リスクとはせず、`review`候補とする。

#### 持ち去り兆候

```text
wallet_8:
approached
  -> contacted
  -> lifted
  -> carried toward torso
  -> disappeared
```

財布は保護対象物であり、持ち上げ、身体側移動、消失が連続している。作業票上の許可遷移にも存在しないため、強い逸脱として判定する。

### 8.7 毀損兆候の扱い

骨格とYOLOトラッキングだけで判定できるのは、主として接近、接触、持ち上げ、移動、持ち去り、消失の兆候である。

物体が破損したか、変形したか、開封されたかを判定するには、対象物体周辺クロップの操作前後比較が必要になる。

毀損兆候を扱う場合は、次を追加する。

- 操作前画像と操作後画像の差分
- 変形、破れ、開封、部品欠落の局所分類
- 急激な手・物体加速度
- 叩く、ねじる、押し潰すなどの姿勢系列
- 正常作業で許可された分解・取り外しとの照合

したがって、YOLOの追跡結果だけを根拠に「毀損を検知した」とは記述しない。

## 9. 予防のための判定タイミング

本研究では、作業後に「不審だった」と記録するだけでは不十分である。

不審行動を未然に防ぐには、早い段階で検知できる必要がある。

そのため、判定タイミングを3段階に分ける。

| 段階 | 見る内容 | 目的 |
| --- | --- | --- |
| 接触前 | 対象外物体へ近づく、手を伸ばす | 予兆検知 |
| 接触中 | 物体に触れる、持ち上げる | 即時検知 |
| 接触後 | 元に戻さない、身体側へ移動する、作業エリア外へ移動する | 強い確認 |

評価では、どの時点で検知できたかを `Time-to-detection` として見る。

## 10. モデル構成

完成形の構成は次の通りである。

```text
人物姿勢推定器
  └─ 骨格系列、姿勢ラベル、手首・体幹軌跡

YOLO検出器 + トラッカー
  └─ 物体ID、bbox、移動、zone、可視状態、局所クロップ

作業票コンパイラ
  └─ 物体ごとの許可行動、許可zone、許可状態遷移、復元条件

人物・物体イベント生成器
  └─ approach / contact / lift / carry / place / return / disappear / exit

物体中心の時系列判定器
  └─ 観測状態遷移と許可状態遷移の照合

リスク統合器
  └─ 逸脱種別、継続時間、対象物リスク、観測信頼度を統合

出力
  └─ normal / review / suspicious / high_risk
     + 根拠
     + 対象物ID
     + 検知時刻
```

研究では、次の二つを比較する。

| 判定方式 | 役割 |
| --- | --- |
| 明示的状態遷移・ルール方式 | 判定根拠を説明できる基準手法 |
| 学習型時系列モデル | 骨格・物体系列から複雑な遷移パターンを学習する提案比較 |

学習型モデルの候補は、LSTM / GRU、Temporal CNN、ST-GCN、Skeleton Transformerである。

ただし、学習型モデルを使用する場合も、作業票の許可対象、許可zone、復元条件は外部制約として残す。学習モデルに許可条件まで暗黙学習させると、作業内容が変わるたびに再学習が必要になり、判定根拠も不明確になるためである。

## 11. 比較実験

比較条件は次の通りである。

| 条件 | 入力 | 目的 |
| --- | --- | --- |
| A | 骨格のみ | 人物の動きだけで判定できるか |
| B | 物体周辺のみ | 物体情報だけで足りるか |
| C | 骨格 + 物体 bbox | 位置関係だけでどこまで判定できるか |
| D | 骨格 + 物体周辺クロップ | 提案手法の基本性能を見る |
| E | 骨格 + 物体周辺 + 作業文脈 | 完成形として文脈の効果を見る |

これにより、どの情報が効いたのかを切り分ける。

VLMに全体画像を入力する方式は主手法に含めず、比較実験では限定視覚情報を用いる本方式との差を評価する。

## 12. 評価指標

評価には次の指標を使う。

| 指標 | 意味 |
| --- | --- |
| Accuracy | 全体でどれだけ当たったか |
| Precision | 不審と判定したものが本当に不審だった割合 |
| Recall | 不審行動をどれだけ見逃さなかったか |
| F1 | Precision と Recall のバランス |
| False positive rate | 正常作業を不審扱いした割合 |
| Time-to-detection | どれだけ早く検知できたか |

この研究では、Accuracy だけでは不十分である。

不審行動は件数が少なくなりやすいため、Recall、False positive rate、Time-to-detection を重視する。

## 13. 保存するデータ

現仕様では、保存するデータをできるだけ制限する。

保存候補:

- 骨格座標
- 関節角度や速度などの特徴量
- 対象物体の bbox
- 対象物体の移動量
- 対象物体周辺の限定クロップ
- 判定結果
- 不審度スコア

できるだけ保存しないもの:

- 顔画像
- 部屋全体の映像
- 作業者の外観特徴
- 不要な生活空間の画像

対象物体周辺のクロップも、必要に応じて低解像度化やマスク処理を行う。

## 14. 現時点の実装状態

現在のactive rootには、Python/Tkinterによるデスクトップアプリの現行実装がある。

旧方針の実装は `legacy/archive_20260616_previous_direction/` に隔離している。

現時点で実装済みの範囲は次の通りである。

- 作業文脈 JSON の読み込み
- 骨格 CSV の読み込み
- 物体追跡 CSV の読み込み
- 物体ごとの不審度スコア計算
- `normal` / `review` / `suspicious` / `high_risk` の分類
- 初回接触時刻、初回警告時刻、Time-to-detection の算出
- GUI での結果表示
- JSON / CSV / Markdown での結果書き出し
- サンプルデータと自動テスト
- 正常系ルーター作業動画から、現行アプリ用の `context.json` / `skeleton.csv` / `object_tracks.csv` / 対象物体周辺クロップ画像を生成する補助スクリプト
- `data/trials/router_normal_img_7852/` の正常系試行データ

現時点で未実装の範囲は次の通りである。

- アプリ本体での動画直接入力
- 汎用的な動画からの骨格自動推定
- 汎用的な動画からの物体検出
- 物体トラッカーによる bbox 自動追跡
- 深層学習モデルによる分類
- カメラ入力からのリアルタイム警告

そのため、現在のアプリは「骨格 CSV と物体追跡 CSV が既にある」という前提で動く。
`scripts/prepare_router_trial.py` は、特定のルーター正常系動画を現行アプリ用データへ変換する補助ツールであり、汎用的な動画解析機能ではない。

今後コードを追加した場合は、この文書を実装内容に合わせて更新する。

## 15. 実行方法

デスクトップアプリを起動する。

```powershell
uv run home-service-verifier-app
```

サンプルデータで CLI 実行する。

```powershell
uv run home-service-verifier-app --context sample_data/task_context.json --skeleton sample_data/skeleton.csv --objects sample_data/object_tracks.csv
```

正常系ルーター作業データで CLI 実行する。

```powershell
uv run home-service-verifier-app --context data/trials/router_normal_img_7852/context.json --skeleton data/trials/router_normal_img_7852/skeleton.csv --objects data/trials/router_normal_img_7852/object_tracks.csv
```

正常系ルーター作業データを元動画から再生成する。

```powershell
uv run --no-project --python 3.11 --with "mediapipe==0.10.21" --with opencv-python-headless python scripts/prepare_router_trial.py --video data/raw_videos/IMG_7852.mp4 --out data/trials/router_normal_img_7852 --sample-seconds 1.0
```

既存 `.venv` がロックされていて `uv run home-service-verifier-app` が失敗する場合は、アプリ本体を直接実行して検証する。

```powershell
$env:PYTHONPATH=(Resolve-Path .\src).Path; python -m home_service_action_verifier.app --context data/trials/router_normal_img_7852/context.json --skeleton data/trials/router_normal_img_7852/skeleton.csv --objects data/trials/router_normal_img_7852/object_tracks.csv
```

結果は `outputs/current_app/<timestamp>/` に保存される。

## 16. 必要ファイルの配置場所

現在のアプリで使う入力ファイルは、次の場所に置く。

| 場所 | 置くもの | アプリで選ぶ項目 |
| --- | --- | --- |
| `data/contexts/` | 作業文脈 JSON | 作業文脈 JSON |
| `data/skeleton/` | 骨格 CSV | 骨格 CSV |
| `data/object_tracks/` | 物体追跡 CSV | 物体追跡 CSV |
| `data/object_crops/` | 対象物体周辺クロップ画像 | 物体追跡 CSV の `crop_path` から参照 |
| `data/raw_videos/` | 元動画 | 現時点では直接入力しない |
| `data/trials/` | 実験ごとの一式 | 各ファイルを個別に選択 |

実験単位でまとめたい場合は、次のように置く。

```text
data/trials/<trial_name>/
  context.json
  skeleton.csv
  object_tracks.csv
  object_crops/
  raw_videos/
```

元動画とクロップ画像はプライバシー情報を含む可能性があるため、Git 追跡しない設定にしている。

## 17. 入力ファイル形式

### 作業文脈 JSON

作業文脈JSONは、単なる物体リストではなく、物体ごとの許可行動、許可zone、許可遷移、復元条件を定義する。

例:

```json
{
  "task_name": "router_repair",
  "work_areas": ["entrance", "router_work_area", "tool_area"],
  "private_areas": ["private_desk", "private_storage"],
  "objects": {
    "router": {
      "role": "target",
      "allowed_zones": ["router_work_area"],
      "allowed_actions": ["approach", "contact", "lift", "carry", "place", "return"],
      "allowed_transitions": [
        ["idle", "approached"],
        ["approached", "contacted"],
        ["contacted", "lifted"],
        ["lifted", "carried"],
        ["carried", "placed"],
        ["placed", "returned"]
      ],
      "must_return": true,
      "return_tolerance_px": 40
    },
    "wallet": {
      "role": "protected",
      "allowed_zones": ["private_desk"],
      "allowed_actions": ["approach"],
      "allowed_transitions": [
        ["idle", "approached"],
        ["approached", "idle"]
      ],
      "must_return": true,
      "alert_on": ["contacted", "lifted", "carried", "disappeared", "exited"]
    }
  },
  "thresholds": {
    "touch_distance_px": 35,
    "body_near_distance_px": 90,
    "minimum_event_frames": 4,
    "track_lost_seconds": 1.0,
    "review_score": 0.25,
    "suspicious_score": 0.55,
    "high_risk_score": 0.80
  }
}
```

この定義から、システムは物体ごとの許可状態遷移を生成する。

### 骨格 CSV

骨格 CSV は次の列を持つ。

```text
timestamp,left_wrist_x,left_wrist_y,right_wrist_x,right_wrist_y,torso_x,torso_y,pose_label
```

### 物体追跡 CSV

物体追跡 CSV は次の列を持つ。

```text
timestamp,object_id,label,role,bbox_x,bbox_y,bbox_w,bbox_h,zone,visible,crop_path
```

`role` には `target`、`worker`、`private`、`high_risk`、`unknown` などを書く。

## 18. 技術的に聞かれたときの短い答え

> 人間は作業前に、触れてよい物体、許可zone、許可される操作順序、復元条件を作業票として定義します。作業中は、人物の骨格・姿勢から手の接近や保持動作を推定し、YOLOで物体のIDと移動を追跡します。両者を統合して、物体ごとに「接近、接触、持ち上げ、運搬、設置、復元、消失」という状態遷移を作り、作業票から生成した許可遷移と一致しない場合に逸脱として検知します。

さらに短く説明する場合は次の通りである。

> 人間が許可条件を決め、システムが骨格とYOLOから実際の状態遷移を作り、両者の不一致を逸脱として検知する。
