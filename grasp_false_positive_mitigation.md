# 2D把持判定の誤検知と対策

## 問題

旧版の把持判定は、手の21点骨格、YOLOで検出した物体bbox、両者の2D画像上の重なりや近接を主に使っていた。

このため、次の2つの状態が画像上でほぼ同じに見える場合がある。

1. 実際に物体を握っている。
2. 手を丸めた状態で、物体の上にかざしているだけ。

2D座標だけでは奥行き方向の前後関係が分からないため、手が物体に接触していなくても、画像上で手と物体が重なると `POWER_OBJECT` のような把持判定が出やすかった。

これは実装上のバグというより、単眼RGBと2Dランドマークに由来する観測限界である。完全な解決には深度情報や複数視点が必要になる。

## 深度カメラ以外の対策

深度カメラは有効だが、追加費用や設置の負担がある。代替として、把持を単発フレームで断定せず、時系列の証拠を追加する。

### 1. 判定状態を細分化する

旧版の `POWER_OBJECT` は、実際には「手が閉じていて、物体bboxと重なっている」状態を含んでいた。

そのため、把持を次の段階に分ける。

```text
APPROACH
HOVER_OVER_OBJECT
CONTACT_LIKELY
GRASP_CANDIDATE
HOLDING_CONFIRMED
CARRYING
```

手が丸まり、物体bboxと重なっただけでは `HOVER_OVER_OBJECT` または `GRASP_CANDIDATE` に止める。

`HOLDING_CONFIRMED` にするには、物体の移動や手との連動など、追加の時系列証拠を要求する。

### 2. 手と物体の連動移動を見る

本当に物体を持っている場合、一定時間、手と物体が同じ方向へ動く可能性が高い。

判定に使う信号は次の通り。

- 手首または掌中心の移動ベクトル
- 指先群の移動ベクトル
- YOLO物体bbox中心の移動ベクトル
- 手と物体bbox中心の距離の安定性
- 連動が継続したフレーム数

例:

```text
手が閉じている
物体bboxと近い
物体中心が手と同じ方向に動く
手と物体の距離が急に離れない
この状態が N フレーム続く
```

この条件を満たしたときだけ `HOLDING_CONFIRMED` にする。

手を物体の上にかざしただけの場合、物体bboxはほとんど動かないため、把持確定まで進まない。

### 3. 物体の移動・消失・bbox変化を証拠にする

深度がなくても、持ち上げや運搬に伴って次の変化が起きることがある。

- 物体bboxの中心が移動する。
- 物体bboxのサイズや縦横比が変わる。
- YOLOの信頼度が変化する。
- 手によって物体が部分的に隠れる。
- 物体が元の場所から消える。

これらを「把持の補助証拠」として使う。

ただし、bbox変化だけでは誤検知もあるため、手の閉じ状態、接触候補、手との連動と組み合わせて使う。

### 4. 単眼推定深度を補助信号にする

深度カメラを使わず、RGB画像から疑似深度を推定する方法もある。

候補:

- Depth Anything
- MiDaS
- ZoeDepth系モデル

これは正確な距離センサーではないが、手が物体より明らかに手前にあるか、物体と近い奥行きにあるかの補助信号として使える。

ただし、処理負荷が増えるため、リアルタイム性とのトレードオフがある。

### 5. 安価な複数視点にする

深度カメラの代わりに、Webカメラやスマホを2台使う。

```text
正面カメラ + 斜め横カメラ
```

一方の視点で手と物体が重なって見えても、別視点では「上にかざしているだけ」と分かる場合がある。

厳密なステレオ復元をしなくても、2視点で同じ把持候補が出たときだけ確定する運用ができる。

### 6. 対象物にマーカーを貼る

研究・実験用途では、対象物にマーカーを貼る方法が現実的である。

候補:

- ArUco marker
- QRコード
- 色付きシール
- 小さな特徴点シール

物体の位置、回転、移動が安定して取れるため、手が重なっただけか、物体が手と一緒に動いたかを判定しやすくなる。

深度カメラより安価で、対象物が限定されている本研究の条件にも合う。

## 現在の実装方針

現段階では、深度カメラではなく時系列判定で対処する。現在の `grip_detector` は、単なる「握っているように見えるか」を最終判定にしない。手の閉じ姿勢と物体接触は候補に留め、同じ追跡物体が同時に移動した場合だけ、把持中移動イベントとしてスコアを上げる。

実装済みの判定入力は次の通り。

1. 手骨格からの把持姿勢スコア。
2. 手先・掌・物体bboxの近接/重なりによる接触スコア。
3. 追跡物体の現在速度 `object_speed_px_s`。
4. 追跡開始位置からの変位 `object_displacement_px`。
5. 速度または変位から作る `object_motion_score`。

最終スコアは、姿勢、接触、移動の3要素を掛け合わせる。したがって、手を丸めて物体上にかざしただけ、または物体が単独で動いただけでは、確定状態へ進みにくい。

静的テクスチャによる幽霊トラック対策では、接触評価を止めない。`settled=False` でも `OBJECT_CONTACT_NO_MOVE` は表示できるようにし、`settled` と `motion_valid` は `object_motion_score` だけを0にするゲートとして使う。元位置に初期テンプレートと同じ見た目が残る場合は vacancy check で `motion_valid=False` とし、偽の把持中移動を抑制する。

今後の優先順位は次の通り。

1. 手と物体bbox中心の移動方向の一致も加え、現在の移動検証をさらに強化する。
2. 移動スコアが一定フレーム続いた場合だけ `HOLDING_CONFIRMED` 相当にする。
3. 物体が作業対象位置から移動した場合に `CARRYING` へ進める。
4. 必要に応じて、単眼深度またはマーカー方式を追加する。

## 結論

2D画像だけでは、手を丸めて物体の上にかざした状態と、実際に物体を握った状態を単発フレームで完全には区別できない。

そのため、単発の姿勢判定ではなく、手と物体の時系列的な連動、物体移動、物体消失、bbox変化を組み合わせる。

本システムでは、当面は次の方針を採る。

```text
手の閉じ + 物体近接 = 把持候補
把持候補 + 追跡物体の速度または変位 = 把持中移動
把持中移動 + 作業位置外移動または未復元 = 運搬・逸脱候補
```

これにより、深度カメラなしでも「重なって見えるだけ」の誤検知を減らせる。
